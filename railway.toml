[build]
builder = "nixpacks"

[nixpacks]
plan = '''
{
  "providers": ["python"],
  "build": {
    "commands": [
      "pip install -r requirements-prod.txt"
    ]
  },
  "start": {
    "cmd": "gunicorn app_complete:app"
  }
}
'''