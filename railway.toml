[build]
builder = "nixpacks"

[nixpacks]
plan = '''
{
  "providers": ["python"],
  "build": {
    "commands": [
      "pip install --upgrade pip",
      "pip install -r requirements.txt"
    ]
  },
  "start": {
    "cmd": "gunicorn minimal_api:app"
  }
}
'''